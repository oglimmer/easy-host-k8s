apiVersion: v1
kind: Service
metadata:
  name: {{ include "easy-host.fullname" . }}
  labels:
    {{- include "easy-host.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easy-host.selectorLabels" . | nindent 4 }}
