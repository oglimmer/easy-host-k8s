spring:
  datasource:
    url: jdbc:h2:mem:easyhost;MODE=MariaDB;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

rate-limit:
  enabled: false
